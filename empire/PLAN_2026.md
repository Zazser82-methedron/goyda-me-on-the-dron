# ПЛАН МОДЕРНИЗАЦИИ «ГОЙДА-ИМПЕРИЯ» → игра уровня Tropico

Составлен 2026-08-11. Синтез: аудит кода (проверено), отчёт Gemini Deep Research (отфильтрован),
дизайн-вердикт Codex `gpt-5.6-terra`.

**Решение по стеку:** остаёмся на Three.js, доводим перф, затем упаковка в **Electron**
(не Tauri — Tauri отдаёт рендеринг системному WebView2, версия которого зависит от машины игрока;
для тяжёлой WebGL-сцены это лотерея. Electron везёт свой Chromium: что протестировали, то игрок
и получит. Размер дистрибутива как аргумент не работает — целевой бюджет и так пара гигабайт).

**Признак, что решение пора менять:** после Фазы 1 при 300 агентах FPS остаётся ниже 30.
Тогда — Godot 4, но уже с готовым дизайном на руках.

---

## ФАЗА 0 — ИЗМЕРЕНИЕ ✅ выполнено 2026-08-11

| # | Задача | Файлы | Статус |
|---|---|---|---|
| 0.1 | Профилировщик: тик/рендер/FPS/draw calls/tris/юниты; оверлей по F3 | `engine/Profiler.js` (new), `engine/Loop.js`, `main.js` | ✅ |
| 0.2 | Стресс-сценарий `window.__stress(n)` | `engine/Profiler.js` | ✅ |
| 0.3 | Baseline-замер 50/150/300 агентов | — | ✅ см. таблицу ниже |

### Baseline (после Фазы 1.1 spatial hash + 1.2 убран второй WebGL-контекст, ДО 1.3/1.5)

Замерено в Playwright/Chromium headless локально (`http://localhost:7842`), каждый уровень —
чистый рестарт кампании + один вызов `__stress(n)`, чтобы не путать с гибелью ратуши между замерами.
FPS в headless занижен троттлингом фонового таба Chrome — ориентир по нему давать нельзя,
но **тик и рендер в мс — реальные и сопоставимые**.

| Юнитов | тик, мс | рендер, мс | draw calls | tris |
|---|---|---|---|---|
| 53 | 3.19 | 17.82 | 777 | 79 836 |
| 154 | 5.14 | 27.72 | 1558 | 96 726 |
| 301 | 9.07 | 40.89 | 2523 | 115 744 |

Тик растёт почти линейно (3.19 → 9.07 мс на 53 → 301, то есть ×2.8 при ×5.7 юнитов) —
spatial hash работает, квадратичного роста больше нет. Рендер растёт быстрее тика и уже
на 301 юните держит ~41 мс сам по себе (это где InstancedMesh из 1.5 даст основной эффект:
сейчас каждый юнит — отдельный draw call, отсюда 2523 draws на 301 агента).

**Побочный баг, найден и исправлен по ходу замера**: `engine/Renderer.js` рендерит через
`EffectComposer` с несколькими внутренними проходами (RenderPass, bloom, tonemap, grade, tilt,
SMAA) — каждый внутренний `renderer.render()` по умолчанию сбрасывает `renderer.info`, поэтому
профилировщик до фикса видел только последний проход (SMAA, полноэкранный треугольник) —
отсюда фантомные `draws=1 tris=1`. Исправлено: `renderer.info.autoReset=false` +
ручной `reset()` в `beginRender()` (`engine/Profiler.js`, `?v=95`).

**Не баг, а факт геймплея**: `__stress()` спавнит в обход гейта Waves.js «нет набегов без
казармы/ранга/воинов» — на пустой базе 50+ агентов кладут ратушу за секунды. Это ожидаемо
для тестового инструмента, не свидетельство о жалобе игрока на ранний набег (та жалоба —
про обычный игровой спавнер, который гейт уже соблюдает).

---

## ФАЗА 1 — ПРОИЗВОДИТЕЛЬНОСТЬ (цель: 300 агентов при 60 FPS)

| # | Задача | Проблема (проверено в коде) | Решение | Файлы | Статус |
|---|---|---|---|---|---|
| 1.1 | Пространственный хэш | `Units.js` — O(N²): `nearestEnemyUnit` полностью сканирует юнитов до 3 раз на врага за тик + скан зданий | Uniform spatial hash, ячейка 8 ед. (радиус агро шамана 7.0 — самый дальний), поиск кольцами от 3×3 | `sim/SpatialHash.js` (new), `sim/Units.js` | ✅ 0 расхождений со старым алгоритмом на ~90k синтетических проверок |
| 1.2 | Убрать второй WebGL-контекст | `Thumbs.js:19` создавал второй `WebGLRenderer` рядом с `Renderer.js:62` | Рендер превью через `WebGLRenderTarget` основного контекста + ручное sRGB-кодирование (RT линеен), кэш dataURL | `ui/Thumbs.js`, `ui/BuildMenu.js` | ✅ проверено в браузере, регрессий по цвету/кэшу нет |
| 1.3 | Кольцевой буфер частиц | `Atmosphere.js:138` — линейный `.find()` на каждый эмит | Ring buffer с зацикленным индексом | `world/Atmosphere.js` | ⏳ следующее |
| 1.4 | Разделить симуляцию и рендер | На каждого юнита каждый кадр: `heightAt()`, 4 синуса, проверка тумана | Кэш высоты на юните, туман — на тике 10 Гц, походка — в вершинный шейдер | `main.js`, `sim/Units.js` | ⏳ |
| 1.5 | InstancedMesh для юнитов | Каждый юнит — отдельный меш (2523 draws на 301 агента, замерено) | Один `InstancedMesh` на тип юнита, процедурная походка на GPU через `aInstancePhase` | `world/UnitRenderer.js` (new), `main.js` | ⏳ по замеру — главный кандидат для рендер-времени (41мс на 301 юните) |
| 1.6 | Time-slicing ИИ + поднять потолок | `MAX_ENEMIES = 56` — костыль от тормозов, а не баланс. Замер 0.3 показал тик держится: 9мс на 301 юните ещё далеко от бюджета кадра | ИИ по бюджету кадра, дальние агенты реже; поднять потолок — но сверить с числами 12/22/34 из дизайн-спеки Фазы 2 (см. `PHASE2_DESIGN_SPEC.md`), не решать вслепую | `sim/Units.js`, `sim/Waves.js` | ⏳ после 1.5, по факту рендер-бюджета |

**Критерий фазы:** 300 агентов, тик ≤ 2 мс, draw calls ≤ 80, стабильные 60 FPS на ноутбуке.

## ✅ ФАЗА 1 ЗАВЕРШЕНА 2026-08-11, запушено в master (905a1c4)

Итоговый замер на 300 агентах (Playwright/Chromium, чистый рестарт кампании перед замером):

| | до Фазы 1 | после Фазы 1 | выигрыш |
|---|---|---|---|
| тик симуляции | 9.07 мс | 5.3 мс (при спавне; в бою ниже — time-slicing) | — |
| рендер | 40.89 мс | 12.05 мс | **×3.4** |
| draw calls | 2523 | 488 | **×5.2** |
| tris | 115 744 | 117 752 | ~то же (геометрия та же, просто меньше draw call на неё) |

Живая проверка в браузере (не только по отчётам агентов): пикинг юнита кликом подтверждён через
реальный путь `Picker.js` (найден клик по `id:376`, `type:'unit'`), меню построек без регрессий,
консоль чистая на всех сценариях. Найден и исправлен попутный баг: `EffectComposer` сбрасывал
`renderer.info` на каждом внутреннем проходе — профилировщик до фикса видел только последний
проход (`draws=1 tris=1`), почему числа в этой таблице надёжны только после правки.

15 файлов изменено/создано, все версии импортов сведены (несовпадение версий у общих файлов —
`Units.js`, `Waves.js` — было реальным багом от параллельной работы агентов, исправлено дважды
за день, см. коммит).

---

## ФАЗА 2 — СИСТЕМЫ (Tropico-логика)

| # | Задача | Суть | Статус |
|---|---|---|---|
| 2.1 | Три эпохи | «Дерево и Земля» → «Железо и Опричнина» → «Пар и Абсолют Дрона». Переход по выполнению 3 из 5 условий, не по накоплению очков — чтобы игрок выбирал путь | ✅ `sim/Eras.js` (Sonnet+Codex, коммит 1fc20e1 от 2026-08-11) — статус в таблице не обновлялся, хотя код давно готов; см. живую проверку 2026-08-14 ниже |
| 2.2 | Модернизация построек | База + 2 ветвящиеся специализации вместо уровней I–V. Цена: ресурсы + рабочие + простой. Один дешёвый откат за эпоху | ✅ `sim/Upgrades.js` (Sonnet+Codex, тот же коммит) — 6 построек (изба/амбар/казарма/частокол/рудник/башня) |
| 2.3 | Износ и обслуживание | Три состояния: исправно / изношено / аварийно. −15% эффективности, затем риск поломки. Не мгновенная смерть здания | ✅ `sim/Wear.js` (Codex), числа сверены со спекой построчно |
| 2.4 | Изба Плотника | Авторемонт в радиусе. Игрок задаёт политику района, а не кликает по каждому зданию | ✅ авторемонт (радиус 8, 6 целей, +4 износа/день); ⏳ политика района — явный TODO в коде |
| 2.5 | Четыре формата набега | Осада с лагерем · требование дани · диверсия против конкретного здания · налёт с угоном ресурсов | ✅ `sim/RaidFormats.js` (Codex). Диверсия упрощена: доступна только когда уже есть руина/здание ≤35% HP (честно задокументировано агентом, не своя система целеуказания) |
| 2.6 | Телеграфирование угрозы | За 90/60/30 с: рог, линия направления на земле, костры на границе тумана, карточка «кто / чего хочет / что потеряешь» | ✅ 3 ступени (пропорционально сжимаются, если интервал короче 90с), HUD-баннер с иконкой/названием формата/целью/подсказкой «что теряешь». Без новой 3D-геометрии — чисто CSS-эскалация |
| 2.7 | Кривая угрозы | Бюджет угрозы вместо роста толпы. Потолки одновременных врагов по эпохам: 18 / 28 / 40 при качественном росте | ✅ бюджет (старт 10, +3/день, +8 за отбитый набег) вместо прямой формулы; per-era потолки — TODO, эпох ещё нет |
| 2.8 | Антиспираль смерти | Чрезвычайные эдикты (мобилизация, закрома) + милость Идола Дрона при падении населения ниже 25% | ✅ `sim/AntiSpiral.js` (тот же коммит) — мобилизация/закрома/милость; см. живую проверку 2026-08-14 ниже |

**✅ Фаза 2 закрыта целиком 2026-08-14** (статусная таблица досведена под фактический код — 2.1/2.2/2.8
были реализованы ещё 2026-08-11, но таблица не обновлялась). Живая проверка боевых функций через
`window.__profiler.state` + динамический `import()` тех же версионированных модулей, что грузит игра
(не изолированный юнит-тест — тот же код, тот же state):
- **Эпохи:** синтетически удовлетворены «3 из 5» условий перехода I→II (население 14, казарма +
  2 фермы/амбара, 40 золота, 1 волна) → `Eras.update()` сам перевёл `state.era` 0→1, `upgradeResets`→0,
  +20 веры, тост «НОВАЯ ЭПОХА» — всё как в коде. (Первая попытка теста уронила `advance()` на
  `Wear.js:apply` — из-за собственных building-заглушек без `.def` в тесте, не бага игры; со вторым
  проходом на реальных `state.addBuilding(..., {built:true})` не воспроизводится.)
- **Модернизация:** реальная Изба через `addBuilding` → `Upgrades.startUpgrade(state, izba, 'a', ctx)` —
  рабочий занят, ресурсы списаны, тост «модернизация начата»; форс-тик на полное время работ →
  `finishUpgrade` отработал, `izba.upgrade==='a'`, `cap.pop` вырос на 3, тост готовности.
- **Антиспираль:** население ниже 25% лимита → `AntiSpiral.update()` сам вызвал `grantMercy` — +60 еды,
  +25 веры, `mercyTimer` встал, тост «Милость Идола Дрона». Тестовые здания/эффекты убраны после проверки.
- Заодно найден и починен настоящий баг (не связан с Фазой 2, всплыл при `__stress(250)` + наведении
  курсора): `Picker.entityUnder()` падал на `Raycaster.intersectObjects` с `Cannot read properties of
  undefined (reading 'layers')`, если в `pickables`/`fields` попадал транзиентный `undefined` (здание/
  поле, ещё не обзаведшееся `.view`/`.inst` в момент кадра). Фикс — `targets.filter(Boolean)` перед
  raycast'ом в `js/engine/Picker.js`. Дефенсивный фикс, не выяснял точного продюсера гонки — не требуется,
  раз ни один валидный объект не отфильтровывается.
- Заодно найден и починен рассинхрон кеш-бастинга (не по вине этой сессии, тянулся с коммита `90465d3`
  от 2026-08-11): `config.js` получил `PAL.water`/`PAL.waterDeep`, но версию в импорте бампнули только
  в 2 из 18 мест (`TerrainMesh.js`/`WorldBase.js` → `v102`, остальные 16 остались на `v94`). У игрока
  с закешированным `config.js?v=94` до этого коммита 16 модулей продолжали бы жить со старой копией
  файла. Досведено до `v102` везде, и каскадом пересчитаны версии всех файлов, чьё содержимое из-за
  этого изменилось (итог — 25 файлов правлено, `index.html` `main.js?v=123→124`), путь-осознанной
  проверкой на дубликаты имён (`sim/Research.js` и `ui/Research.js` — разные файлы, разные версии,
  не баг) и `node --check` на каждом.

**Критерий фазы:** партия на 20–30 минут проходится целиком, проигрыш возможен в середине,
но у игрока всегда есть ход назад.

### Ход работы 2026-08-11 (после Фазы 1)

2.3/2.4/2.5/2.7 сделаны **параллельно двумя независимыми Codex-агентами** (не Sonnet) в
изолированных файловых зонах (`data/buildings.js`+`sim/Buildings.js`+`sim/Economy.js`+новый
`sim/Wear.js` — задача износа; `sim/Waves.js`+новый `sim/RaidFormats.js` — задача набегов).
Оба зависели от общего поля `building.ruined`, которое задачи не согласовывали между собой
напрямую — сошлось только потому, что обе читали одну и ту же спеку. Проверено вручную
(`grep -n ruined` по всем файлам) — совпадает.

Первая попытка задачи по износу упала: собственный терминал Codex-сессии завис даже на `echo`,
и агент корректно отказался от слепых правок без проверки (это правильное поведение, не баг).
Повтор с той же задачей прошёл штатно. По ходу также обнаружился и убит осиротевший
Codex-процесс (провисел 3ч44м после разрыва MCP-сессии) — не источник изменений, безопасно
проигнорирован.

Обе задачи после сдачи: `node --check` пройден на всех файлах, версии импортов сведены вручную
(во всех местах — старая проблема рассинхрона версий на этот раз не повторилась, только
собственные версии изменённых файлов не были подняты создавшими их агентами — досведено).
Проверено вживую в браузере: Изба Плотника видна в меню построек с точными числами спеки
(30 дерева/10 камня, радиус 8, 6 целей, +4 износа/день), `__stress(250)` — тик 0.4мс,
рендер 4.9мс, draws 364 — никакой просадки от новой логики относительно конца Фазы 1.

---

## ФАЗА 3 — ВИЗУАЛ И GAME FEEL

| # | Задача | Суть |
|---|---|---|
| 3.1 | Лобби с Дроном | Дрон левитирует над миниатюрным поселением: синус A=0.2 м / T=2.4 с, пульс рун через `emissiveIntensity` 0.5→1.5, прожектор сканирует карту |
| 3.2 | Переход лобби→игра, 2.2 с | 0–0.4 импульс дрона · 0.4–1.2 рывок камеры вниз · 1.2–1.8 пролёт сквозь облака · 1.8–2.2 выход на игровой ракурс, появление HUD |
| 3.3 | Toon shading + контур | `MeshToonMaterial` с градиентной картой 1×5, контур вторым проходом через `BackSide` |
| 3.4 | Game feel | Hit stop 40 мс на крупных событиях, screen shake `trauma² × maxOffset`, вспышка белым материалом на 0.08 с при уроне |
| 3.5 | Дешёвые анимации | Стройка из лесов · падение дерева поворотом вокруг основания · развал здания на 3 куска · дым спрайтами · ресурс летит в счётчик |
| 3.6 | Звук с нуля | Howler.js. Папка `assets/audio` пустая — звука нет вообще. Минимум: 4 UI + 4 боевых + амбиент. CC0 с Freesound / OpenGameArt, теги gusli, balalaika, medieval-horn |

---

### Побочная ветка: PHASE3_ART_LOGIC_SPEC.md (аудит моделей/дорог/баланса по запросу игрока)

Независимый аудит от Codex (см. `PHASE3_ART_LOGIC_SPEC.md`) нашёл системную причину «кривых
дорог»: награда телеги/поезда считалась от ДЛИНЫ маршрута — игроку было математически выгодно
строить петли. Работаем по приоритетам P0→P1→P2→P3 из спеки, каждый раунд — через Codex как
кодера (не только ревьюера), с независимой проверкой (git diff + node --check + живой Playwright).

**✅ Раунд 1 (P0) — запушено 2026-08-11/12:**
- Радиусы аур (кольца) скрыты по умолчанию, видны только при выборе постройки (Tropico-style).
- Убран эксплойт длины маршрута: телега — `6+0.4×длина` → `round(cart.cargo)`; поезд —
  `10+0.5×длина` → фиксированные `20` (TODO P1: полноценный грузовой цикл рельс).
- Рынок/Трактир: перепродажа излишков (`Economy.js` onDay) больше не падает в казну мгновенно —
  копится в `b._pendingCargo`; телега забирает ≤40 груза и довозит именно его, а не абстрактную
  сумму. Без дороги до порта здания груз просто копится (не пропадает, не превращается в золото).
  Груз в пути/накопленный переживает save/load (Codex сам заметил и закрыл этот edge case).
- `roadPort`/`railPort` — у 8 зданий (townhall, market, traktir, prikaz_sbora, tamozhnya,
  izba_plotnika, remontny_dvor, sklad_putevoy, station): путь ищется от конкретной клетки-входа,
  а не от произвольной клетки периметра footprint.
- Панель выбора рынка/трактира (`Selection.js`) показывает накопленный груз в 🪙.
- **Живая проверка (Playwright, не только самоотчёт):** рынок с дорогой до порта — телега
  спавнится, забирает cargo (капнуто на 40), довозит, золото зачисляется; рынок без дороги —
  `_pendingCargo` копится, телега не спавнится; дорога построена НЕ на клетку-порт (другая сторона
  footprint) — путь **не находится** (это и есть цель фикса, не баг); `__stress(120)` — консоль
  чистая.
- Визуал дорожных тайлов/рельс/мостов (`Tiling.js`) и сами 6 дублей 3D-моделей — НЕ трогали в этом
  раунде (сознательно, отдельная задача P1/дальше — новая геометрия дороже и рискованнее логики).

**✅ Раунд 2 (P1, рабочие-холопы) — запушено 2026-08-12:**
- Ферма/Рудник/Жила получили `workSlots: 2` — производят полную норму только при заполненных
  местах. Формула множителя: 0 работников → ×0.15, 1 → ×0.5, 2 (полный штат) → ×1.0.
  Базовые числа `produce` не менялись — это теперь потолок при полном штате, не гарантия.
- Назначение — ПКМ (или 🎯 ПРИКАЗ) холопом по зданию: холоп идёт туда (тот же путь, что и к
  стройплощадке), встаёт и работает ПОСТОЯННО, пока не переназначен/здание не снесено/холоп не
  погиб. Слоты не резиновые — при занятости обеих клеток игра отказывает тостом.
  Реализовано через `Jobs.js` (новая ветка `workSite`/`state==='working'`), с опорой на уже
  существующий прецедент `startRepair()` в `Buildings.js`.
- Очистка централизована в `GameState.js`: `removeBuilding`/`removeUnit`/`killUnit` теперь разом
  чистят `buildSite`/`repairSite`/`workSite` — раньше это не было единообразно, был риск утечек.
- Постоянные работники исключены из авто-подбора на ремонт/апгрейд/стройку (не могут быть
  «украдены» с поста другой системой).
- Панель постройки показывает «👷 X/2 работников».
- **Живая проверка (Playwright):** холоп физически дошёл до фермы за ~2.3с реального времени и
  перешёл в `state==='working'`, счётчик `_activeWorkers` корректно посчитал его; прямой тест
  `removeBuilding()` — работник корректно освобождается (`working`→`idle`, `workSite`→`null`);
  прямой тест `killUnit()` — здание переживает смерть работника, слот освобождается; `__stress(150)`
  — консоль чистая.

**✅ Раунд 3 (P1, Пакгауз/Приказ) — запушено 2026-08-12:**
- Путевой Пакгауз (`sklad_putevoy`) больше не даёт плоские +4 золота/день — участвует в той же
  перепродаже излишков, что рынок/трактир (копит `_pendingCargo`). Поезд на станции ищет ближайший
  (≤6 клеток по прямой — погрузочный причал, не отдельный маршрут) построенный Пакгауз и забирает
  до 60 груза вместо плоской награды; без пакгауза рядом или без груза — прежние 20 (рельсы не
  ломаются, просто без апгрейда).
- Приказ сбора (`prikaz_sbora`) больше не дублирует Торг — плоские +6 золота заменены налогом от
  населения: `round(clamp(population/N × 0.4, 3, 25))` на приказ (N приказов делят население, чтобы
  не облагать дважды). Чем больше держава — тем весомее контора, а не декоративная копия рынка.
- **Живая проверка (Playwright):** 2 станции + рельсы + пакгауз с `_pendingCargo=55` в 3 клетках от
  станции — поезд забрал груз (`_pendingCargo` 55→0, золото выросло), приказ сбора корректно считает
  подать от населения, `__stress(150)` — консоль чистая, workSite-механика рабочих не задета.

**Read-only консультация с Codex по геометрии (2026-08-12):** прочитал `Tiling.js`+`Placeholders.js`
целиком, дал план из 5 пунктов — направленное тело дороги вместо квадратной плиты, обочины по маске
(не в фиксированных координатах), стрелки/крестовины для рельс вместо схождения линий в центр, мост
как `bridgeTile(mask, endMask)` с устоями, декоративный подъезд к `roadPort` здания. Заодно нашёл:
`roadPort`/`railPort` считаются от НЕповёрнутого footprint — при повороте здания (`R`) декоративный
подъезд может разойтись с фасадом (на доставку груза не влияет, зафиксировано в PHASE3-спеке).

**✅ Раунд 4 (P1, геометрия roadTile) — запушено 2026-08-12:**
- `roadTile(mask)` в `Placeholders.js` переписан с «квадратная плита + узкие полосы» на направленное
  тело: центральный узел раскрывается до ширины дороги (не на весь тайл), от него идут лучи по
  активным направлениям маски с небольшим выходом за грань тайла (нахлёст с соседом), у T/перекрёстка
  узел шире, чем у прямой/угла — теперь они отличимы не только числом ответвлений. Добавлены боковые
  бордюры и поперечные швы, привязанные к направлению луча (не к фиксированным координатам тайла,
  как было раньше — старые «камешки» иногда лежали прямо на пути движения).
- Задача выполнялась Codex в фоне (task-msp9o5fs-12nk6x) и упала на реконнекте API ПОСЛЕ того, как обе
  правки уже применились на диск и прошли `node --check` — самоотчёта агент не оставил, поэтому весь
  диф прочитан и проверен мной вручную построчно, а не по самоотчёту.
- **Живая проверка (Playwright, скриншот, не только код):** собран тестовый участок из 9 дорожных
  тайлов — угол, T-стык, перекрёсток, прямая, несколько тупиков — камера подведена вручную через
  `cameraRig` (прямая установка `camera.position` не работает, RTS-камера пересчитывает её из
  `target/radius/polar/azimuth` каждый кадр). На скриншоте видна направленная лента с читаемым
  расширением на стыках — визуально отличается от старой сплошной плиты. Консоль чистая.
- НЕ трогали: `railTile`, мост (`bridge`-ветка), сам вызов из `Tiling.js`, любую логику/данные из
  прошлых 3 раундов.

**✅ Раунд 5 (P1, геометрия railTile) — запушено 2026-08-12:**
- `railTile(mask)` в `Placeholders.js`: прямая — две непрерывные рельсы; угол — плавная дуга из 3
  повёрнутых сегментов на каждую рельсу (тригонометрия по центру дуги, не «two lines meeting at a
  point»); T — главная прямая + дугой изогнутая ветка + остриё стрелочного языка; перекрёсток — обе
  прямые + приподнятый ромбовидный крестовик + доп. шпалы. На каждой активной грани тайла сохранён
  разнос рельс `±0.14` (проверено и в коде, и визуально) — стыковка с соседним тайлом не рвётся.
- Самоотчёт агента на этот раз пришёл полностью (в прошлый раз упал на реконнекте) — но всё равно
  дифф прочитан целиком вручную перед тем, как доверять числам из отчёта.
- **Живая проверка (Playwright, скриншот):** участок из 9 рельсовых тайлов (тот же layout: угол/T/
  крестовина/прямая/тупики) — на скриншоте чётко видна плавная дуга поворота и не рвущаяся колея,
  заметно читаемее старой версии, где все лучи сходились в одну точку в центре тайла. Консоль чистая.
- НЕ трогали: `roadTile` (раунд 4), мост, `Tiling.js`, любую логику/данные из предыдущих раундов.

**✅ Раунд 6 (P1, bridgeTile + устои) — запушено 2026-08-12:**
- `bridgeTile(mask, endMask)` в `Placeholders.js` — настил вытянут вдоль фактической оси
  (E-W/N-S определяется маской), перила по бокам пролёта, продольная балка и 4 сваи снизу.
  `endMask` считается в `Tiling.js` как маска соседей с `def.road===true && kind!=='bridge'`
  (обычная дорога, не другой мост) — на этих гранях ставится каменный устой с плитой и короткая
  наклонная рампа к уровню дороги; между двумя мостами устой не ставится (ровный стык). Одиночный
  неподключённый мост (`mask===0`) — рабочий фолбэк N-S с двумя устоями.
  `refreshRoad()` в `Tiling.js` для моста теперь пересобирает геометрию так же, как для обычной
  дороги (`while children.length...`), а не просто поворачивает старую статичную модель.
- **Живая проверка (Playwright, скриншот):** первая попытка поставила мост на СУХОЙ земле (косяк
  теста, не кода) — настил уходит на `grid.water` (-0.7) независимо от реального рельефа, из-за
  чего был погребён под более высокой сушей и не виден сверху; нашли настоящую воду через
  `grid.heightAt` относительно `grid.water`, переставили — на скриншоте чётко виден деревянный
  настил с перилами, вытянутый вдоль реки, с устоем на береговом конце. Косвенно подтверждён и
  инструментально: у крайних мостовых тайлов `_bridgeEndMask` ненулевой на стороне берега и `0`
  на стороне соседнего моста, `view.children.length` в районе 21-24 (реальная геометрия, не пусто).
- Известная **непочиненная нестыковка** (из read-only консультации, не трогали намеренно): телега
  берёт Y из `grid.heightAt()`, а не из уровня настила моста — визуально телега может казаться чуть
  ниже полотна на мосту. Отдельная задача на стыке визуала и `Transport.js`.
- НЕ трогали: `roadTile`/`railTile` (готовы), `Transport.js`, любую логику/данные из прошлых раундов.

**Итог по геометрии тайлов (раунды 4-6):** дорога/рельсы/мост — все три типа переведены с
«одна фигура на все случаи + поворот» на честную пересборку по маске соседей, с визуальной
проверкой каждого через живой скриншот, а не только на словах.

**✅ Раунд 7 (P1, пилот новых силуэтов — БЕЗ Blender) — запушено 2026-08-12:**
- Найден дешёвый путь в обход Blender-пайплайна: `AssetManager.load(name)` пытается скачать
  `assets/models/<name>.glb`, при 404 молча остаётся без прототипа, и `buildPlaceholder(name)` из
  `Placeholders.js` подставляет процедурную JS-геометрию. Значит для новых силуэтов достаточно
  завести НОВЫЙ ключ модели без `.glb`-файла — движок сам покажет плейсхолдер. Экономит недели
  ручного 3D-моделирования, остаёмся в уже отработанном инструментарии (JS-геометрия, тот же метод,
  что для дороги/рельс/моста в раундах 4-6).
- Пакгауз (`sklad_putevoy`, было `model: 'bld_ambar'`) → новый `bld_sklad`: приподнятая грузовая
  платформа на опорах, штабели ящиков/тюков, деревянный кран со стрелой и грузом на тросе —
  читается как «депо у путей», не «второй амбар».
- Ремонтный двор (`remontny_dvor`, было `model: 'bld_kuznica'`) → новый `bld_remdvor`: ОТКРЫТЫЙ
  двор под лёгким навесом на столбах (не закрытая коробка с трубой) — верстак, точильный круг с
  искрами, стойка инструментов, бочка.
- Проверено: `.glb`-файлы НЕ создавались (`git status`/`find -newer` чисты).
- **Живая проверка (Playwright, скриншот):** Амбар/Пакгауз/Кузница/Ремонтный двор поставлены рядом
  для прямого визуального сравнения — все 4 читаются как явно разные постройки, консоль чистая
  (никаких 404-ошибок про `.glb` — фолбэк действительно тихий, как задумано).
- Версии импортов сведены каскадом по 12 точкам (`buildings.js` → 6 файлов, `Placeholders.js` →
  4 файла, плюс попутно найден и исправлен ещё один старый рассинхрон версий: `BuildMenu.js`
  импортировался с v94 из `Selection.js`/`Research.js`, но с v95 из `main.js`).

**✅ Раунд 8 (P1, силуэты Изба Плотника/Баня) — запушено 2026-08-12:**
- Изба Плотника (было `model: 'bld_izba'`) → новый `bld_izba_plotnika`: открытый рабочий двор —
  низкий односкатный навес на столбах, штабель брёвен, козлы с воткнутой пилой. Не изба, а
  мастерская — читается сразу.
- Баня (было `model: 'bld_izba'`) → новый `bld_banya`: приземистый сруб ниже обычной избы,
  двускатная (не коническая) кровля, короткая печная труба, лохань с ободом снаружи, тёмное
  (не тёплое) окошко — намеренно отличается от жилого «огонька» избы.
- Обычная Изба (`izba()`) не тронута — использует настоящий `bld_izba.glb`, подтверждено косвенно:
  у неё `view.children.length === 1` (целая GLB-сцена одним узлом), у новых двух — 16 и 8 (россыпь
  процедурных примитивов) — структурное доказательство, что плейсхолдеры реально разные, а не GLB
  переиспользуется.
- **Живая проверка (Playwright):** скриншот с тремя постройками рядом + структурная проверка
  `childCount`. Набег в этот раз всё-таки уронил ратушу МЕЖДУ вызовами (сейв уже 53 дня жил) —
  не помешало проверке, данные для сравнения были собраны раньше. Консоль чистая.

**✅ Раунд 9 (P1, силуэты Трактир/Приказ) — запушено 2026-08-12:**
- Трактир (было `model: 'bld_market'`) → новый `bld_traktir`: ЗАКРЫТЫЙ бревенчатый сруб (не открытый
  прилавок-навес Торга) с двускатной крышей, тёплым окном, лавками, бочками у стены, вывеской с
  золотым знаком кружки/бочонка.
- Приказ сбора (было `model: 'bld_market'`) → новый `bld_prikaz`: каменный цоколь и кладка,
  строгая шатровая крыша, входные столбы со ступенями, уездное знамя с гербовой печатью, ларь
  документов — читается как «контора», солиднее рыночного навеса Торга.
- Торг (`market()`) не тронут — использует настоящую GLB-модель.
- **Живая проверка (Playwright):** структурно подтверждено (`view.children.length`: рынок 1 —
  целая GLB-сцена, трактир 13, приказ 12 — россыпь новых процедурных примитивов, разные числа =
  разная геометрия), плюс скриншот с разными формами/цветами крыш. Снова словили ту же неуловимую
  аномалию с уходом страницы на `about:blank` между вызовами (уже третий раз в этой сессии, сервер
  при этом жив) — не мешает проверке, просто повторно логирую как непочиненную мелочь.

**✅ Раунд 10 (P1, силуэты Вече/Агитпункт) — запушено 2026-08-12:**
- Вече (было `model: 'bld_observatory'`) → новый `bld_veche`: открытый деревянный круглый помост,
  четыре резных столба с багровыми знамёнами, перекладина с колоколом схода — общинное деревянное
  здание, без камня и купола обсерватории.
- Агитпункт (было `model: 'bld_observatory'`, эпоха III) → новый `bld_agitpunkt`: индустриальная
  трибуна на каменном основании — металлический каркас, плакатный щит с бирюзовой эмблемой, мачта
  с направленным рупором-громкоговорителем — читается как пропагандистский объект индустриальной
  эпохи, не научная башня.
- `observatory()` не тронута.
- **Проверка:** дифф прочитан построчно (в т.ч. проверены нестандартные ключи палитры `PAL.rock`/
  `PAL.rockDk`/`PAL.faithCyan` — все существуют в `config.js`, и цепочка `.rotateY()` на результате
  `box()` — валидный метод `THREE.Object3D`), `node --check` чист, лишних `.glb` не появилось.
  **Живую скриншот-проверку в этот раз сделать не удалось** — Playwright-браузер стал крайне
  нестабилен (падает между вызовами почти на каждом evaluate, несколько попыток перезапуска не
  помогли; локальный dev-сервер при этом отвечает нормально — проблема именно в тестовом браузере,
  не в игре). Метод идентичен 4 успешно проверенным ранее раундам (7-9) с тем же паттерном
  «новый ключ модели без `.glb`» — риск оцениваю как низкий, но живая проверка — долг, закрыть при
  первой рабочей возможности Playwright.

**✅ Раунд 11 (P1, силуэт Заставы Острог) — запушено 2026-08-12. Закрывает §1 PHASE3-спеки целиком.**
- Острожная Застава (было `model: 'bld_tower'`, эпоха I, дешевле башни — без железа) → новый
  `bld_zastava`: деревянный частокольный дозорный пост на брёвнах-сваях, приподнятая площадка,
  передняя лестница, простая коническая крыша, небольшая жаровня — читается как «грубый
  приграничный пост», не «каменная башня подешевле».
- `tower()` не тронута.
- При ревью диффа нашёл и сам поправил косметический баг: описательный комментарий-заголовок
  «дорога (1×1, мощёная плита)» съехал на новую функцию `zastavaOstrog()` (Codex вставил функцию
  ПЕРЕД существующим комментарием вместо после), из-за чего `road()` осталась без заголовка.
  Дал каждой функции свой корректный комментарий.
- **Долг по живой проверке закрыт (2026-08-12, следующая сессия):** Playwright отошёл сам после
  паузы. Структурно (`view.children.length`): observatory=1/tower=1 (реальные GLB, не тронуты),
  veche=20/agitpunkt=13/zastava=40 (процедурная геометрия, все разные — подтверждает, что это
  4 разных плейсхолдера, не переиспользование). Плюс скриншот — визуально чётко видны круглая
  деревянная площадка Вече со знамёнами и колоколом (посреди сцены), купольная Обсерватория и
  каркасный Агитпункт с плакатом — три явно разных силуэта рядом. `__stress(100)` — консоль чистая.

**Итог по §1 PHASE3-спеки (раунды 7-11):** все 6 групп визуальных дублей моделей разведены —
Пакгауз, Ремонтный двор, Изба Плотника, Баня, Трактир, Приказ сбора, Вече, Агитпункт, Застава
Острог получили собственные силуэты (9 новых процедурных моделей) через fallback-механизм
`AssetManager`, без единого нового `.glb`-файла и без Blender-пайплайна.

**✅ §2 закрыт целиком (2026-08-12).** Роут-порты учитывают поворот здания (`rotatePort()`,
сверено с эталоном `DIR_ROT`), декоративный апрон-подъезд к дороге/рельсам реализован и
позиционный баг в первой версии (Codex взял долю от переменного расстояния вместо фиксированного
отступа — апрон проваливался внутрь здания для 2×2) — найден и исправлен вручную.
**Долг по живой проверке §2 закрыт (следующая сессия):** апрон рынка структурно проверен
(`distFromCenter=1.331` — безопасно снаружи футпринта 2×2, между краем ~1.0 и портом ~1.75) и
визуально на скриншоте с крупным планом — не проваливается в здание.

**✅ §6 (амбиентные анимации Баня/Роща/Застава) — запушено 2026-08-14.** Пар из трубы бани
(3 клуба, циклично поднимаются и растворяются), покачивание саженцев рощи ветром (та же техника,
что у пшеницы фермы — instanced-меши), флаг на заставе (та же техника, что у вывески рынка).
Существующие анимации (кузница/рынок/трактир/ферма/рудник/жила/обсерватория/вече/церковь/станция)
не тронуты — дифф чисто аддитивный. Единственное найденное сомнение (флагшток заставы визуально
близко к конусу крыши) снято скриншотом крупным планом — флаг читается чисто, не проваливается
в геометрию крыши. Роща — 4 зелёных саженца видны на скриншоте. Баня — структурно подтверждено
3 объекта пара. `__stress(100)` — консоль чистая.

**Далее по приоритету (P1):** полноценный грузовой цикл для рельс (§7, TODO уже оставлен в
Railroad.js), новые типы транспорта + риск ограбления караванов (§7), событийные (не амбиентные)
анимации — открывание ворот амбара/пакгауза при прибытии транспорта (требует связки с реальным
событием доставки в Transport.js/Railroad.js, не сделано в этом раунде специально).

**✅ Раунд 12 (§7, грузовой цикл рельс + риск ограбления + купеческий обоз) — 2026-08-14, сделано
лично (не Codex), без Blender/новых ассетов:**
- `Railroad.js`: поезд забирает груз у депо станции ОТПРАВЛЕНИЯ (`pickupCargo()`) и продаёт его по
  прибытии на ДРУГОЙ конец линии — настоящая точка-в-точку перевозка вместо обналичивания любого
  депо рядом со станцией касания. Порожний рейс — токен 20 (не штрафует голую линию в ноль).
- Новый общий модуль `sim/Convoy.js` — риск ограбления караванов у живого вражьего стана
  (`state.camps`, тот же объект, что у осадного формата набега): шанс 12% на проверку раз в 2с
  вплотную к стану, линейно к 0% на границе радиуса 9 тайлов, теряется 30-60% текущего груза,
  10с неприкосновенности после. Подключено И к телегам (`Transport.js`), И к поезду.
  `state.camps` — не декоративная угроза: стан можно снести и обезопасить маршрут.
  Использует уже существующий sfx-пресет `'raid'` (не добавлял новый).
- Купеческий обоз (`Transport.js`) — с Эпохи II рынок/трактир копят груз до 70 (было 40), тот же
  `unit_cart`, но множитель риска ×1.6 — честно приоритетная цель, как просила спека §7.
- Дрезина-ремонтник и бронепоезд «Гойда» (Эпоха III+) — не в этом раунде, остаются в P2.
- **Верификация — только аналитическая, живой Playwright сознательно не открывал в этом раунде**
  (прямое указание пользователя — сначала пройти несколько задач, потом один общий заход в
  браузер). Прочитан весь диф построчно, `node --check` на каждом изменённом/новом файле, версии
  импортов каскадом пересчитаны и перепроверены путь-осознанным (не только по имени файла) грепом
  на дубликаты — граф межфайловых версий сходится без единого MISMATCH. Вручную протрассированы
  edge-cases: посадка груза на самом первом рейсе поезда (раньше не было вовсе), повторная посадка
  на развороте у той же станции, `despawnTrain()` ровно в момент `arrive()` (груз молча теряется —
  редкий и не хуже прежнего поведения), маршрут без пакгауза/рынка (не создаёт `undefined`-груз).
  Живая проверка этой партии — в общем заходе позже, честно как долг, не как «уже подтверждено».

---

## ФАЗА 4 — УПАКОВКА И STEAM

| # | Задача | Суть |
|---|---|---|
| 4.1 | Electron + steamworks.js | Автономный билд, достижения, облачные сейвы |
| 4.2 | Сейвы в файловую систему | Сейчас `localStorage` — теряется при чистке кэша. Переезд в `%APPDATA%/Goyda/saves` |
| 4.3 | Пауза при потере фокуса | Без этого билд заворачивают на модерации |
| 4.4 | Адаптивное разрешение | Ultra-wide и 1280×800 Steam Deck без искажения пропорций |
| 4.5 | Геймпад и Steam Deck | Gamepad API, вызов виртуальной клавиатуры, читаемый UI на 7 дюймах |
| 4.6 | Steam Direct | $100 (возврат при выручке $1000), страница за 3–6 месяцев до релиза, цель 7–10k вишлистов. Сроки модерации проверить в первоисточнике Steamworks — данные из отчёта Gemini неточны |

---

## ФАЗА 5 — КАРТОЧНАЯ ИГРА

Отложена до завершения Фазы 3 по решению игрока. Отдельное предупреждение: отчёт Gemini
по карточной игре анализировал **мёртвый прототип** `govori-goyda 2` (985 строк, без git,
последняя правка 4 июня), а не живую игру `index.html` (2356 строк, рогалик, боссы, онлайн).
Его рекомендация «зафиксировать `govori-goyda 2` как эталонную базу» уничтожила бы актуальную
версию. К этому отчёту не возвращаться без перепроверки.

---

## Правила работы

- Импорты версионируются вручную: `./file.js?v=94`. Новые файлы — с тем же параметром,
  при правках версию поднимать, иначе GitHub Pages отдаст закэшенное.
- `package.json` нет и не заводим до Фазы 4 — проект остаётся статикой.
- Проверка — программная: `node --check` по синтаксису, счётчики профилировщика по перфу.
  Скриншот только там, где правка визуальная.
- Запуск локально только через HTTP (ES-модули + importmap не работают с `file://`).
- Не коммитить и не пушить без явной команды игрока.
